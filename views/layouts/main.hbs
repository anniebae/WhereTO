<!DOCTYPE html>
<html lang="en">
<head>
{{>head}}
</head>
<body>
  {{#if user}}
	  {{>_app-navbar}}
  {{else}}
    {{>_welcome-navbar}}
  {{/if}}
	
	{{{body}}}
	
	{{>templates}}
	{{>scripts}}
  </body>
</html>